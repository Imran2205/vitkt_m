[VITKT_M]  # <tracker-name>
label = VITKT_M
protocol = traxpython
command = test_tracker_vot
# Specify a path to trax python wrapper if it is not visible (separate by ; if using multiple paths)
paths = [full-path-to-vitkt_m]
env_PATH= [env-path-to-vitkt_m];${PATH}
